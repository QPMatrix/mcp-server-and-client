{
  "name": "server",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "inspect": "set DANGEROUSLY_OMIT_AUTH=true && bunx @modelcontextprotocol/inspector bun dev",
    "build:server": "tsc",
    "build:watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/inspector": "^0.16.1",
    "@modelcontextprotocol/sdk": "^1.16.0"
  }
}
